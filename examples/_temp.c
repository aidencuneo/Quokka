#include "../include/t.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

int _FORVARNAME;
int main(int argc,char**argv){
integer num=Integer(10);
while(Bool(num).value.value){
println(num);
num=num.__sub__(num,Integer(Integer(1)));
}
}
