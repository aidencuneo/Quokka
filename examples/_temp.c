#include "../include/t.h"
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

int _FORVARNAME;
int main(int argc,char**argv){
if(10){
string word=String("aiDEn");
println(word.upper(word));
println(word.lower(word));
}
}
